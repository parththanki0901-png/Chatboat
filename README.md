# Chatboat 🤖
A FastAPI chatbot powered by Google Gemini.

## Setup
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Add your Gemini API key in `.env`
4. Run: `uvicorn app.main:app --reload`